import React from "react";

const NoGifs = props => (
  <li className="no-gifs">
    <i className="material-icons icon-gif">sentiment_very_dissatisfied</i>
    <h3>
      Sorry, no GIFs match your search. Try again in Dave's Famous Gif Search
      Bar above.
    </h3>
  </li>
);

export default NoGifs;
